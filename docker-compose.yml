version: '3.8'

services:
  # ----------------------------
  # [TODO] 候选人请在此处补充配置
  # ----------------------------

  # 示例结构 (请取消注释并修改):

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"

  # backend:
  #   build: ./backend
  #   ports:
  #     - "8000:8000"

  # db:
  #   image: postgres:15 (or mysql/redis)
  #   ...
